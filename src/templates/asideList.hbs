{{#each results}}
<li class="best-movies__item" data-id="{{this.id}}">
  <div data-id="{{this.id}}" class="best-movies__img-wrapper">
  {{#if this.poster_path}}
  <img data-id="{{this.id}}" width="350" height="400" class="card__img" src="https://image.tmdb.org/t/p/w300{{this.poster_path}}" />
  {{/if}}
  {{#unless this.poster_path}}
  <img data-id="{{this.id}}" width="350" height="400" alt="no-poster" class="card__img" src="./images/no-poster.png" />
  {{/unless}}
  </div>
  <div data-id="{{this.id}}" class="best-movies__text-wrapper">
    <h3 data-id="{{this.id}}" class="best-movies__headline">{{this.title}}</h3>
    <div data-id="{{this.id}}" class="best-movies__rating">
      <span data-id="{{this.id}}" class="rating__text">{{this.vote_average}}</span>
    </div>
  </div>
</li>
{{/each}}